<atlassian-plugin key="${atlassian.plugin.key}" name="${project.name}" plugins-version="2">
    <plugin-info>
        <description>${project.description}</description>
        <version>${project.version}</version>
        <vendor name="${project.organization.name}" url="${project.organization.url}" />
        <param name="plugin-icon">images/pluginIcon.png</param>
        <param name="plugin-logo">images/pluginLogo.png</param>
        <param name="configure.url">/secure/admin/LogViewerConfig!default.jspa</param>
    </plugin-info>

    <!-- add our i18n resource -->
    <resource type="i18n" name="i18n" location="jira-log-viewer" />

    <!-- add our web resources -->
    <web-resource key="jira-log-viewer-resources" name="jira-log-viewer Web Resources">
        <directory>static</directory>
        <resource type="download" name="jira-log-viewer.css" location="css/jira-log-viewer.css" />
    </web-resource>

    <rest key="jira-log-viewer-rest" path="/logviewer" version="1.0">
        <package>com.atsoft.jira.plugin.logviewer.rest</package>
        <security>authenticated</security>
    </rest>
</atlassian-plugin>